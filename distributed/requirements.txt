# Distributed training dependencies
# Install on ALL devices (server + workers):
#   pip install -r distributed/requirements.txt

torch>=2.0.0
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pyngrok>=7.0.0
requests>=2.31.0
numpy>=1.24.0
pydantic>=2.0.0
